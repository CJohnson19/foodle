package com.example.foodle.model;

/**
 * Enum that's used to pass Intent to {@link com.example.foodle.SearchActivity} when
 * calling from another Activity.
 */
public enum FilterCategory {
    RECIPES,
    INGREDIENTS,
}
